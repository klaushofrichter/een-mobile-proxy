{
  "name": "een-mobile-proxy-root",
  "version": "0.1.0",
  "private": true,
  "description": "EEN Mobile OAuth Proxy - for iOS/Android apps",
  "scripts": {
    "prepare": "husky",
    "test": "./scripts/run-all-tests.sh",
    "test:proxy": "cd proxy && npm test",
    "test:perf": "cd proxy && npm run test:perf",
    "test:admin": "cd admin && npm test",
    "deploy:proxy": "cd proxy && npm run deploy"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3",
    "gh-pages": "^6.3.0",
    "husky": "^9.1.7"
  }
}
