{
  "name": "een-mobile-proxy",
  "version": "1.3.31",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run stop && node scripts/set-local-version.js && wrangler dev --ip 127.0.0.1 --port 3333",
    "stop": "lsof -ti :3333 | xargs kill -9 2>/dev/null || echo 'Port 3333 is free'",
    "deploy": "node scripts/deploy.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:perf": "vitest run test/performance.test.js",
    "prepare": "cd .. && husky"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.12",
    "dotenv": "^17.2.3",
    "husky": "^9.1.7",
    "vitest": "^3.2.0",
    "wrangler": "^4.65.0"
  }
}
