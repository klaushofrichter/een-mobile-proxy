# Production Configuration
# Copy this file to .env.prod for use with: npm run dev:prod
#
# No VITE_PROXY_URL needed — the admin SPA is served from the same
# origin as the proxy via Cloudflare Workers Static Assets.
